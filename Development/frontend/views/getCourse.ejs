<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Viewing one course</title>
  </head>
  <body>
    <h1>Viewing Content of <%= course.courseName %></h1>
    <div>
        <% for(let content of course.courseContent){ %>
          <p><%= content.creatorName%></p>
          <p><%= content.courseContentDescription %></p>
          <button><a href=<%= `/courses/view/${course._id}/${content._id}` %> >View Details</a></button>
        <% } %>
    </div>
  </body>
</html>
